我将根据您的要求，将提示机制从“底部横幅”彻底改造为“右上角极简律动漏斗”。

**核心方案设计：**

1.  **位置调整 (Position)**：
    *   从右下角 (`bottom: 24px`) 移至 **右上角** (`top: 24px`)。
    *   固定定位，确保在任何页面滚动位置都能看到。

2.  **视觉重构 (Visuals - The "Funnel")**：
    *   **图标**：将 Loading 状态的图标替换为 **漏斗 (Funnel/Filter)**。
    *   **动画**：为漏斗添加 **"律动" (Pulse/Beat)** 动画，模拟呼吸或筛选数据的节奏感。
    *   **极简主义**：**移除所有文字**，移除背景色块（或改为极简透明背景），移除关闭按钮。只保留一个纯粹的、动态的图标悬浮在页面右上角。

3.  **交互逻辑 (Interaction)**：
    *   **状态流转**：
        *   **处理中**：显示律动的漏斗。
        *   **结束时**：当任务完成（进入“下个界面”或状态变更），漏斗立即消失（或转换为极简的成功/失败图标后快速消失，以确保用户知道结果，但严格遵循“不加多余字”）。
        *   *策略*：为了极致体验，我会设置在任务成功时，漏斗会短暂变为一个绿色的“对勾”然后立即淡出消失；出错时变为红色“感叹号”然后消失。全程无文字。

**修改文件：**
*   `trae-extension/background.js`

**技术实现细节：**
*   CSS Keyframes 新增 `traePulse` (律动效果)。
*   修改 SVG 为漏斗形状。
*   隐藏 Text 节点。

是否确认执行此方案？
